import {
  font,
  resize,
  color,
  hp,
  wp,
  wps,
  square,
  width,
  height,
  hps,
} from 'globalStyle';
import { StyleSheet } from 'react-native';
export default styles = color =>
  StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: '#fff',
  },
});
